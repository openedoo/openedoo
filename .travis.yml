language: python
sudo: required
python:
  - "2.7"
before_install:
    - pip install pytest pytest-cov
    - pip install -r requirements.txt

# command to install dependencies
install: "python manage.py module unittest -a"

script: python manage.py test
